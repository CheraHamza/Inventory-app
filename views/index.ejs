<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Library</title>
		<link rel="stylesheet" href="css/index.css" />
	</head>
	<body>
		<h1>Books</h1>
		<ul>
			<% if(locals.books) { %> <% books.forEach(book => { %>
			<li>
				<img width="150px" src="<%= book.cover_url  %>" alt="" />
				<p><b>Title : </b> <%= book.title %></p>
				<p><b>Pages : </b> <%= book.length %></p>
				<p><b>Published on : </b> <%= book.published_year %></p>
				<p><b>ISBN : </b> <%= book.isbn %></p>
				<p><b>Status : </b> <%= book.status %></p>
				<p><b>Description : </b> <%= book.description%></p>
				<a href="<%= book.more_details_url  %>">More info</a>
			</li>
			<hr />
			<% }); %> <% } %>
		</ul>
	</body>
</html>
